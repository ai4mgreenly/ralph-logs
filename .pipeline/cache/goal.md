Story: #0

## Objective

Create a file named `PING.md` in the root of the repository with the content `PONG` (just the word, nothing else).

## Acceptance Criteria

- A file named `PING.md` exists in the repository root
- The file contains exactly `PONG` followed by a newline
- No other files are modified